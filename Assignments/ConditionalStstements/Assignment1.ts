//Check if a character is uppercase
let character: string = "a";

if (character >= "A" && character <= "Z") {
    console.log(`${character}  is UPPERCASE Character`)
}
else {
    console.log(character, ": is not a UPPERCASE Character")
}